name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag, for example v0.1.0
        required: true
        type: string
      release_name:
        description: Release title
        required: false
        default: ""
        type: string
      platform:
        description: Release platform
        required: false
        default: "linux"
        type: choice
        options:
          - linux
          - windows
      toolchain:
        description: Build toolchain
        required: false
        default: "gcc"
        type: choice
        options:
          - gcc
          - mingw
          - msvc
      qt_version:
        description: Qt version
        required: false
        default: "6.8.3"
        type: string
      qt_arch:
        description: Qt architecture, leave empty to auto-select
        required: false
        default: ""
        type: string
      prerelease:
        description: Mark release as prerelease
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  build-linux:
    if: ${{ inputs.platform == 'linux' }}
    runs-on: ubuntu-22.04

    env:
      BUILD_TYPE: Release
      PACKAGE_ROOT: ${{ github.workspace }}/package
      PACKAGE_DIR: ${{ github.workspace }}/package/ZcAILib
      ARCHIVE_NAME: ZcAILib-${{ inputs.tag }}-linux-gcc.tar.gz

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ninja
        uses: seanmiddleditch/gha-setup-ninja@v5

      - name: Resolve Qt architecture
        id: qt_arch
        shell: bash
        run: |
          qt_arch="${{ inputs.qt_arch }}"
          if [ -z "$qt_arch" ]; then
            qt_arch="gcc_64"
          fi
          echo "qt_arch=$qt_arch" >> "$GITHUB_OUTPUT"
          echo "Using Qt architecture: $qt_arch"

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ inputs.qt_version }}
          host: linux
          target: desktop
          arch: ${{ steps.qt_arch.outputs.qt_arch }}
          cache: true

      - name: Configure
        shell: bash
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE="$BUILD_TYPE"

      - name: Build
        shell: bash
        run: cmake --build build --config "$BUILD_TYPE"

      - name: Install package layout
        shell: bash
        run: cmake --install build --config "$BUILD_TYPE" --prefix "$PACKAGE_DIR"

      - name: Create archive
        shell: bash
        run: |
          tar -C "$PACKAGE_ROOT" -czf "$ARCHIVE_NAME" ZcAILib

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARCHIVE_NAME }}
          path: ${{ github.workspace }}/${{ env.ARCHIVE_NAME }}

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: ${{ inputs.release_name != '' && inputs.release_name || inputs.tag }}
          prerelease: ${{ inputs.prerelease }}
          generate_release_notes: true
          files: ${{ github.workspace }}/${{ env.ARCHIVE_NAME }}

  build-windows:
    if: ${{ inputs.platform == 'windows' }}
    runs-on: windows-2022

    env:
      BUILD_TYPE: Release
      PACKAGE_DIR: ${{ github.workspace }}\package\ZcAILib
      ARCHIVE_NAME: ZcAILib-${{ inputs.tag }}-windows-${{ inputs.toolchain }}.zip

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSVC
        if: ${{ inputs.toolchain == 'msvc' }}
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup MSYS2
        if: ${{ inputs.toolchain == 'mingw' }}
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ninja

      - name: Validate toolchain
        if: ${{ inputs.toolchain == 'gcc' }}
        shell: pwsh
        run: |
          Write-Error "Windows builds only support toolchain=mingw or toolchain=msvc."

      - name: Resolve Qt architecture
        id: qt_arch
        shell: pwsh
        run: |
          $qtVersion = "${{ inputs.qt_version }}"
          $qtArch = "${{ inputs.qt_arch }}"
          $toolchain = "${{ inputs.toolchain }}"

          if ([string]::IsNullOrWhiteSpace($qtArch)) {
            if ($toolchain -eq "mingw") {
              $qtArch = "win64_mingw"
            } else {
              $parts = $qtVersion.Split('.')
              $major = if ($parts.Length -gt 0) { [int]$parts[0] } else { 0 }
              $minor = if ($parts.Length -gt 1) { [int]$parts[1] } else { 0 }

              if ($major -gt 6 -or ($major -eq 6 -and $minor -ge 8)) {
                $qtArch = "win64_msvc2022_64"
              } elseif ($major -eq 6 -or ($major -eq 5 -and $minor -ge 15)) {
                $qtArch = "win64_msvc2019_64"
              } elseif ($major -eq 5 -and $minor -ge 9) {
                $qtArch = "win64_msvc2017_64"
              } elseif ($major -eq 5 -and $minor -ge 6) {
                $qtArch = "win64_msvc2015_64"
              } else {
                $qtArch = "win64_msvc2013_64"
              }
            }
          }

          "qt_arch=$qtArch" >> $env:GITHUB_OUTPUT
          "Using Qt architecture: $qtArch for toolchain: $toolchain"

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ inputs.qt_version }}
          host: windows
          target: desktop
          arch: ${{ steps.qt_arch.outputs.qt_arch }}
          cache: true

      - name: Configure MSVC
        if: ${{ inputs.toolchain == 'msvc' }}
        shell: cmd
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=%BUILD_TYPE%

      - name: Build MSVC
        if: ${{ inputs.toolchain == 'msvc' }}
        shell: cmd
        run: cmake --build build --config %BUILD_TYPE%

      - name: Install MSVC package layout
        if: ${{ inputs.toolchain == 'msvc' }}
        shell: cmd
        run: cmake --install build --config %BUILD_TYPE% --prefix "%PACKAGE_DIR%"

      - name: Configure MinGW
        if: ${{ inputs.toolchain == 'mingw' }}
        shell: msys2 {0}
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE="${BUILD_TYPE}" \
            -DCMAKE_PREFIX_PATH="$(cygpath -u "$QT_ROOT_DIR")"

      - name: Build MinGW
        if: ${{ inputs.toolchain == 'mingw' }}
        shell: msys2 {0}
        run: cmake --build build --config "${BUILD_TYPE}"

      - name: Install MinGW package layout
        if: ${{ inputs.toolchain == 'mingw' }}
        shell: msys2 {0}
        run: |
          cmake --install build \
            --config "${BUILD_TYPE}" \
            --prefix "$(cygpath -u "$PACKAGE_DIR")"

      - name: Create archive
        shell: pwsh
        run: |
          $archivePath = Join-Path $env:GITHUB_WORKSPACE $env:ARCHIVE_NAME
          if (Test-Path $archivePath) {
            Remove-Item $archivePath -Force
          }

          Compress-Archive `
            -Path $env:PACKAGE_DIR `
            -DestinationPath $archivePath `
            -CompressionLevel Optimal

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARCHIVE_NAME }}
          path: ${{ github.workspace }}\${{ env.ARCHIVE_NAME }}

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: ${{ inputs.release_name != '' && inputs.release_name || inputs.tag }}
          prerelease: ${{ inputs.prerelease }}
          generate_release_notes: true
          files: ${{ github.workspace }}\${{ env.ARCHIVE_NAME }}
